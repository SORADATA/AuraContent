- name: 🔍 Test de l'API Zernio (Debug)
        run: python -c "import os, requests; key = os.getenv('ZERNIO_API_KEY'); print('Clé présente:', bool(key)); headers = {'Authorization': f'Bearer {key}', 'Content-Type': 'application/json'}; res = requests.get('https://zernio.com/api/v1/analytics?sortBy=engagement&limit=5', headers=headers); print('Status:', res.status_code); print('Body:', res.json())"
