name: Générateurs de Vidéos IA

on:
  workflow_dispatch:
    inputs:
      topic:
        description: "Sujet de la vidéo (laisser vide pour choix automatique par l'IA)"
        required: false
        default: ""

      duration:
        description: "Durée souhaitée en secondes (20 à 90)"
        required: false
        default: "45"

      refine_angle:
        description: "Laisser l'IA reformuler l'angle du sujet"
        required: false
        default: "true"

      use_hook_variants:
        description: "Tester plusieurs hooks avant génération"
        required: false
        default: "true"

  repository_dispatch:
    types: [trigger-videos]


jobs:

  # ==============================================================
  # FINANCE
  # DÉSACTIVÉ VOLONTAIREMENT
  # ==============================================================
  finance_generator:
    if: false

    runs-on: ubuntu-latest

    # Anciennement 45 min.
    # Augmenté pour éviter que GitHub termine le job pendant
    # une génération longue d'images / audio / vidéo.
    timeout-minutes: 90

    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}

      HF_TOKEN: ${{ secrets.HF_TOKEN }}

      ZERNIO_API_KEYS_FINANCE: ${{ secrets.ZERNIO_API_KEYS_FINANCE }}

      PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}

      POLLINATIONS_TOKEN: ${{ secrets.POLLINATIONS_TOKEN }}

      VIDEO_TOPIC: ${{ github.event.inputs.topic || '' }}
      VIDEO_DURATION: ${{ github.event.inputs.duration || '45' }}
      REFINE_ANGLE: ${{ github.event.inputs.refine_angle || 'true' }}
      USE_HOOK_VARIANTS: ${{ github.event.inputs.use_hook_variants || 'true' }}

      WIKIMEDIA_CONTACT: ${{ secrets.WIKIMEDIA_CONTACT }}

      # Évite certains comportements Python liés au buffering
      PYTHONUNBUFFERED: "1"

    steps:

      # ------------------------------------------------------------
      # CHECKOUT
      # ------------------------------------------------------------
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v4


      # ------------------------------------------------------------
      # FFMPEG
      # ------------------------------------------------------------
      - name: ⚙️ Installer FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg


      # ------------------------------------------------------------
      # POLICE
      # ------------------------------------------------------------
      - name: 🔤 Installer la police pour le watermark
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-dejavu-core


      # ------------------------------------------------------------
      # PYTHON
      # ------------------------------------------------------------
      - name: 🐍 Configurer Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"


      # ------------------------------------------------------------
      # WHISPER
      # ------------------------------------------------------------
      - name: 🎙️ Installer whisper.cpp
        run: |
          git clone --depth 1 https://github.com/ggerganov/whisper.cpp.git

          cd whisper.cpp

          bash ./models/download-ggml-model.sh base

          make -j"$(nproc)"


      # ------------------------------------------------------------
      # CACHE HUGGING FACE
      # ------------------------------------------------------------
      - name: 🗃️ Cache Hugging Face
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface

          key: ${{ runner.os }}-huggingface-${{ hashFiles('requirements.txt') }}

          restore-keys: |
            ${{ runner.os }}-huggingface-


      # ------------------------------------------------------------
      # DEPENDANCES
      # ------------------------------------------------------------
      - name: 📦 Installer les dépendances
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt


      # ------------------------------------------------------------
      # GENERATION FINANCE
      # ------------------------------------------------------------
      # FIX : chaque tentative est désormais bornée par `timeout` (coreutils),
      # avec envoi de SIGKILL après un délai de grâce si le process ignore le
      # SIGTERM initial (cas classique d'un sous-processus ffmpeg bloqué).
      # Avant ce fix, le "timeout-minutes: 85" du step s'appliquait à la
      # BOUCLE ENTIÈRE : si la tentative 1 se bloquait indéfiniment, elle
      # consommait à elle seule tout le budget et les tentatives 2 et 3
      # n'avaient jamais lieu (c'est exactement ce qu'on observait dans les
      # logs : "Tentative 1 / 3" puis plus rien jusqu'au timeout global).
      - name: 🎬 Générer la vidéo (Finance)
        timeout-minutes: 85
        run: |
          set -o pipefail

          # Budget par tentative en secondes. 3 tentatives x 1500s = 4500s
          # (75 min) + marge pour les pauses, sous le plafond de 85 min du
          # step. Ajuste ATTEMPT_TIMEOUT_S si tes rendus légitimes durent
          # normalement plus longtemps.
          ATTEMPT_TIMEOUT_S=1500

          for i in 1 2 3; do

            echo ""
            echo "============================================================"
            echo "🎬 Tentative $i / 3 (timeout par tentative : ${ATTEMPT_TIMEOUT_S}s)"
            echo "============================================================"
            echo ""

            START_TIME=$(date +%s)

            timeout --signal=TERM --kill-after=30 "${ATTEMPT_TIMEOUT_S}" \
              python -u main_finance.py 2>&1 | tee "generation_finance_attempt_${i}.log"
            EXIT_CODE=${PIPESTATUS[0]}

            END_TIME=$(date +%s)
            DURATION=$((END_TIME - START_TIME))

            echo ""
            echo "⏱️ Durée tentative $i : ${DURATION}s"
            echo "📌 Code retour : ${EXIT_CODE}"

            if [ "$EXIT_CODE" -eq 124 ] || [ "$EXIT_CODE" -eq 137 ]; then
              echo "⏱️ Tentative $i tuée pour dépassement du timeout par tentative."
            fi
            echo ""

            if [ "$EXIT_CODE" -eq 0 ]; then
              echo "============================================================"
              echo "✅ Génération Finance réussie"
              echo "============================================================"
              exit 0
            fi

            echo "============================================================"
            echo "❌ Échec de la tentative $i"
            echo "============================================================"

            if [ "$i" -lt 3 ]; then
              echo "⏳ Pause de 15 secondes avant nouvelle tentative..."
              sleep 15
            fi

          done

          echo ""
          echo "============================================================"
          echo "❌ Les 3 tentatives Finance ont échoué."
          echo "============================================================"

          exit 1


      # ------------------------------------------------------------
      # LOGS DES TENTATIVES
      # ------------------------------------------------------------
      - name: 💾 Sauvegarder les logs Finance
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Logs-Finance-${{ github.run_id }}
          path: generation_finance_attempt_*.log
          if-no-files-found: warn


      # ------------------------------------------------------------
      # LOUDNESS
      # ------------------------------------------------------------
      - name: 🔊 Vérifier le niveau sonore
        if: always()
        run: |
          if [ -f assets/final/final_short.mp4 ]; then

            ffmpeg \
              -i assets/final/final_short.mp4 \
              -af loudnorm=print_format=summary \
              -f null - 2>&1 \
              | tee loudness_finance.txt

          else

            echo "Aucune vidéo finale trouvée." \
              | tee loudness_finance.txt

          fi


      # ------------------------------------------------------------
      # RAPPORT LOUDNESS
      # ------------------------------------------------------------
      - name: 💾 Sauvegarder le rapport (Finance)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Loudness-Report-Finance-${{ github.run_id }}
          path: loudness_finance.txt
          if-no-files-found: warn


      # ------------------------------------------------------------
      # VIDEO
      # ------------------------------------------------------------
      - name: 💾 Sauvegarder la vidéo (Finance)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Video-TikTok-Finance-${{ github.run_id }}
          path: assets/final/final_short.mp4
          if-no-files-found: warn


      # ------------------------------------------------------------
      # CAPTION
      # ------------------------------------------------------------
      - name: 💾 Sauvegarder la légende (Finance)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Caption-Texte-Finance-${{ github.run_id }}
          path: caption.txt
          if-no-files-found: warn



  # ==============================================================
  # MINUTE MYSTERE
  # ==============================================================
  mystere_generator:

    runs-on: ubuntu-latest

    # IMPORTANT :
    # 45 minutes était trop juste puisque ton dernier run
    # atteignait environ 43 minutes avant "operation canceled".
    timeout-minutes: 90

    env:

      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}

      HF_TOKEN: ${{ secrets.HF_TOKEN }}

      ZERNIO_API_KEY: ${{ secrets.ZERNIO_API_KEY }}

      PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
      PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}

      VIDEO_TOPIC: ${{ github.event.inputs.topic || '' }}
      VIDEO_DURATION: ${{ github.event.inputs.duration || '45' }}

      REFINE_ANGLE: ${{ github.event.inputs.refine_angle || 'true' }}
      USE_HOOK_VARIANTS: ${{ github.event.inputs.use_hook_variants || 'true' }}

      WIKIMEDIA_CONTACT: ${{ secrets.WIKIMEDIA_CONTACT }}

      # Force les logs Python à apparaître immédiatement
      PYTHONUNBUFFERED: "1"


    steps:

      # ------------------------------------------------------------
      # CHECKOUT
      # ------------------------------------------------------------
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v4


      # ------------------------------------------------------------
      # FFMPEG
      # ------------------------------------------------------------
      - name: ⚙️ Installer FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg


      # ------------------------------------------------------------
      # POLICE WATERMARK
      # ------------------------------------------------------------
      - name: 🔤 Installer la police pour le watermark
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-dejavu-core


      # ------------------------------------------------------------
      # PYTHON
      # ------------------------------------------------------------
      - name: 🐍 Configurer Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"


      # ------------------------------------------------------------
      # CACHE HUGGING FACE
      # ------------------------------------------------------------
      - name: 🗃️ Cache Hugging Face
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface

          key: ${{ runner.os }}-huggingface-${{ hashFiles('requirements.txt') }}

          restore-keys: |
            ${{ runner.os }}-huggingface-


      # ------------------------------------------------------------
      # DEPENDANCES
      # ------------------------------------------------------------
      - name: 📦 Installer les dépendances
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt


      # ------------------------------------------------------------
      # GENERATION MINUTE MYSTERE
      # ------------------------------------------------------------
      # FIX : timeout dur par tentative (voir commentaire équivalent dans le
      # job Finance ci-dessus). C'est ce fix qui aurait dû limiter les dégâts
      # même avant la correction de composer.py : ta dernière exécution n'a
      # montré que "Tentative 1 / 3" dans les logs, preuve que la tentative
      # 1 a mangé tout le budget de 85 min sans jamais rendre la main à la
      # boucle bash.
      - name: 🎬 Générer la vidéo (Minute Mystere)
        timeout-minutes: 85
        run: |
          set -o pipefail

          # Budget par tentative en secondes. Ajuste si tes rendus légitimes
          # dépassent normalement 25 min (par ex. beaucoup de scènes/variants).
          ATTEMPT_TIMEOUT_S=1500

          for i in 1 2 3; do

            echo ""
            echo "============================================================"
            echo "🎬 Tentative $i / 3 (timeout par tentative : ${ATTEMPT_TIMEOUT_S}s)"
            echo "============================================================"
            echo ""

            START_TIME=$(date +%s)

            timeout --signal=TERM --kill-after=30 "${ATTEMPT_TIMEOUT_S}" \
              python -u main.py 2>&1 | tee "generation_mystere_attempt_${i}.log"
            EXIT_CODE=${PIPESTATUS[0]}

            END_TIME=$(date +%s)
            DURATION=$((END_TIME - START_TIME))

            echo ""
            echo "⏱️ Durée tentative $i : ${DURATION}s"
            echo "📌 Code retour : ${EXIT_CODE}"

            if [ "$EXIT_CODE" -eq 124 ] || [ "$EXIT_CODE" -eq 137 ]; then
              echo "⏱️ Tentative $i tuée pour dépassement du timeout par tentative."
            fi
            echo ""

            if [ "$EXIT_CODE" -eq 0 ]; then

              echo "============================================================"
              echo "✅ Génération Minute Mystere réussie"
              echo "============================================================"

              exit 0

            fi

            echo "============================================================"
            echo "❌ Échec de la tentative $i"
            echo "============================================================"

            if [ "$i" -lt 3 ]; then
              echo "⏳ Pause de 15 secondes avant nouvelle tentative..."
              sleep 15
            fi

          done

          echo ""
          echo "============================================================"
          echo "❌ Les 3 tentatives Minute Mystere ont échoué."
          echo "============================================================"

          exit 1


      # ------------------------------------------------------------
      # LOGS
      # ------------------------------------------------------------
      - name: 💾 Sauvegarder les logs (Minute Mystere)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Logs-MinuteMystere-${{ github.run_id }}
          path: generation_mystere_attempt_*.log
          if-no-files-found: warn


      # ------------------------------------------------------------
      # LOUDNESS
      # ------------------------------------------------------------
      - name: 🔊 Vérifier le niveau sonore
        if: always()
        run: |

          if [ -f assets/final/final_short.mp4 ]; then

            ffmpeg \
              -i assets/final/final_short.mp4 \
              -af loudnorm=print_format=summary \
              -f null - 2>&1 \
              | tee loudness_general.txt

          else

            echo "Aucune vidéo finale trouvée." \
              | tee loudness_general.txt

          fi


      # ------------------------------------------------------------
      # RAPPORT LOUDNESS
      # ------------------------------------------------------------
      - name: 💾 Sauvegarder le rapport (Minute Mystere)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Loudness-Report-MinuteMystere-${{ github.run_id }}
          path: loudness_general.txt
          if-no-files-found: warn


      # ------------------------------------------------------------
      # VIDEO
      # ------------------------------------------------------------
      - name: 💾 Sauvegarder la vidéo (Minute Mystere)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Video-TikTok-MinuteMystere-${{ github.run_id }}
          path: assets/final/final_short.mp4
          if-no-files-found: warn


      # ------------------------------------------------------------
      # CAPTION
      # ------------------------------------------------------------
      - name: 💾 Sauvegarder la légende (Minute Mystere)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Caption-Texte-MinuteMystere-${{ github.run_id }}
          path: caption.txt
          if-no-files-found: warn
